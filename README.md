# Rideshare Scheduler (Backend)

Node/Express backend for scheduling, pricing, and availability checks.

## Requirements
- Node.js (v18+ recommended)
- npm
- PostgreSQL (local or hosted)

## Setup
1. Install dependencies:
   ```bash
   npm install
